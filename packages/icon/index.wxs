
function rootClass (data) {
  var classes = []
  if (data.iconClass) { 
    classes.push(data.iconClass)
  }
  if (data.icon) { 
    classes.push('ipm-icon' + data.icon)
  }
  return classes.join(' ')
}

module.exports.rootClass = rootClass
