<wxs
  src="./index.wxs"
  module="computed"
/>
<wxs
  src="../common/wxs/utils.wxs"
  module="utils"
/>
<wxs
  src="../common/wxs/type.wxs"
  module="type"
/>

<view
  class="ipm-icon {{ [ computed.rootClass({ icon, iconClass}), utils.bem('icon') ] }}"
  style="{{utils.style({ fontSize: utils.matchUnit(size) ? size : size + 'px'})}}"
  bind:tap="click"
/>