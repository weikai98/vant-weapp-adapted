<wxs
  module="utils"
  src="../common/wxs/utils.wxs"
></wxs>


<view
  wx:if="{{show}}"
  class="{{ utils.bem('modal') }}"
  bind:tap="onModalClick"
  catch:touchmove="{{lockScroll ? 'touchmove' : ''}}"
>
  <slot />
</view>