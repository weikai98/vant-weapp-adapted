<wxs
  module="utils"
  src="../common/wxs/utils.wxs"
></wxs>


<view
  wx:if="{{show}}"
  class="{{ utils.bem('modal', [{transparent}]) }}"
  style="z-index: {{zIndex}}"
  bind:tap="onMaskClick"
  catch:touchmove="{{lockScroll ? 'touchmove' : ''}}"
>
  <slot />
</view>